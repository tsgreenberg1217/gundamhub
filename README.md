### git clone --recurse-submodules {url}
### make sure you have an OPEN AI key set as OPEN_AI_KEY in your env
### run gundam-hub-build.sh
### hit /ask directly via postman or use the GundamHub_app react native app
